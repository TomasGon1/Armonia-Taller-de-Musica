<h1>Bienvenido al Taller de Música 🎶</h1>

<div class="carousel">
    {{#each news}}
        <div class="slide">
            <h2>{{this.title}}</h2>
            <p>{{this.content}}</p>

            {{#if this.linkToContact}}
                <a href="/contacto" class="btn">Consultar</a>
            {{/if}}
        </div>
    {{/each}}
</div>